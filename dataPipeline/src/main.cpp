#include <iostream>


// # To configure the Release build (in a new build/release directory)
// cmake --preset release

// # To compile it
// cmake --build build/release



int main() {
    std::cout << "Hello World" << std::endl;
    return 0;
}